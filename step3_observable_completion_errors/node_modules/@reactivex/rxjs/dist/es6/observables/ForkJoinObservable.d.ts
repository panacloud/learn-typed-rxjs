import { Observable } from '../Observable';
import { Subscriber } from '../Subscriber';
export declare class ForkJoinObservable<T> extends Observable<T> {
    private sources;
    constructor(sources: Array<Observable<any> | Promise<any> | ((...values: Array<any>) => any)>);
    static create(...sources: Array<Observable<any> | Promise<any> | ((...values: Array<any>) => any)>): Observable<any>;
    private getResultSelector();
    _subscribe(subscriber: Subscriber<any>): void;
}
